spring:
  datasource:
    url: jdbc:mysql://localhost:3306/srm_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

logging:
  level:
    com.srm: debug
    org.springframework.security: debug
